import React, { Component } from 'react';

class Home extends Component {
    render() {
        return (
            <div>A crash course in CSS Transforms + Transitions</div>
        )
    }
}

export default Home;